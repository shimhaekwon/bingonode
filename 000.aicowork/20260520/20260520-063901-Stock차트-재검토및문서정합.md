---

* 작성일시: 2026-05-20T06:39:01+09:00
* 모델: Claude Opus 4.7 (1M context)
* 작업프로젝트:
  * E:\workspace\bingonode
* 작업 단위: Stock 차트 캔들 주기 패치 재검토 + `docs/stock-spec.md` 문서 정합
* 직전 처리:
  * [./20260520-061429-Stock차트-캔들주기지원.md](./20260520-061429-Stock차트-캔들주기지원.md) (캔들 주기 D/W/M/Y 지원)
* **상태**: 재검토 결과 기능적 결함 0건. 문서 정합 1건(stock-spec.md) 정정 완료.

---

# 세션 이력: 2026-05-20 — Stock 차트 패치 재검토 + 문서 정합

## 작업 대상

- 패치 대상: `3fd4d4d` (캔들 주기 지원)
- 정합 대상: `docs/stock-spec.md`
- 적용 원칙: 6-6 원칙 (`E:\project\.claude\6_6-basis.md`)

## 1. 재검토 수행 — 엣지 케이스 검증

직전 패치(D/W/M/Y 캔들 주기 지원)에 대해 처음 보는 시각으로 6개 엣지 케이스 검증.

| # | 케이스 | input | output | 판정 |
|---|---|---|---|---|
| 1 | 정상 D/W/M/Y (005930.KS) | period 4종 순차 | 315 / 263 / 241 / 28 candles — 모두 다른 데이터 | ✅ |
| 2 | 잘못된 period (`'X'`) | `{ticker, period:'X'}` | 200, `period_echo='D'`, count=315 | ✅ 폴백 |
| 3 | period 누락 | `{ticker}` | 200, `period_echo='D'` | ✅ 기본값 |
| 4 | ticker 누락 | `{period:'W'}` | 400 `ticker is required` | ✅ |
| 5 | invalid ticker | `INVALID_999.KS` | 200, success:true, count:0 | ⚠️ silent (사전 존재 UX) |
| 6 | 짧은 역사 종목 | 051910.KS Y | 26 candles graceful | ✅ |
| 7 | 캐시 hit | 동일 호출 2회 | 268ms → 1ms (268× speedup) | ✅ |
| 8 | predict 회귀 | predict {005930.KS} | status 200, success:true | ✅ |

## 2. 발견사항 분류

### 🟡 패치가 만든 잔여 결함 (1건) — 본 세션에서 정정

**`docs/stock-spec.md` 가 옛 계약(`days`)을 기준으로 작성된 채 남아있음.**

- L44: `/data with days=365` — 옛 계약
- L67: `stockService.getStockData(ticker, days)` — 시그니처 불일치
- L79: `getStockData(ticker, days): delegates to fetcher.fetch(ticker, days)` — 메서드 호출 잘못
- L120: `cacheKey = ticker_periodDays` — 신규 `candles_*` 키 누락

→ 패치 자체가 만든 사실관계 불일치. **6-6 "틀림없이"** 적용 → 정정.

### 🟢 사전 존재 (본 세션 범위 아님 — 가이드로만 기록)

| 항목 | 비고 |
|---|---|
| `config/stocks.js` L11 — `051910.KS` 라벨이 "LG Energy Solution"인데 실제 종목은 LG Chem (LGES는 `373220.KS`) | 데이터 라벨 오류 |
| Yahoo 빈 응답 시 `{success:true, count:0}` — 조용한 실패 | 정책 결정 필요 |
| `stockData.length===0`일 때 `showChart`/`changeTimeframe`이 기존 차트를 destroy 안 함 — 이전 종목 차트 잔존 | UX |
| 빠른 D→W→M 연타 시 늦은 응답이 최신 선택 덮을 수 있는 이론적 레이스 | 요청 토큰 패턴 필요 |
| `_aggregateFromDaily('W')` UTC 일요일 시작 — 한국 시장은 월~금 | 폴백 경로만 |

사용자 결정으로 본 세션은 **stock-spec.md 정합만** 진행.

## 3. 수정 상세 — `docs/stock-spec.md`

```diff
- Fetches prediction (`/predict`) and, if needed, OHLCV data (`/data` with days=365).
+ Fetches prediction (`/predict`) and, if needed, OHLCV candles (`/data` with `period='D'` 일봉 기본).

- `getStockData`: proxies to `stockService.getStockData(ticker, days)`.
+ `getStockData`: validates `period ∈ {D,W,M,Y}` (기본 `D`) → `stockService.getStockData(ticker, period)`. 응답에 `period` echo 포함.

- `getStockData(ticker, days)`: delegates to `fetcher.fetch(ticker, days)`.
+ `getStockData(ticker, period)`: delegates to `fetcher.fetchCandles(ticker, period)` — 캔들 주기(일/주/월/년) 지원. 차트 전용 경로이며 `predict` 는 별도 `fetcher.fetch(ticker, 400)` 사용 (무수정).
```

`stockFetcher.js` 섹션에 진입점 2종 명시:
```diff
+ 두 종류의 진입점이 있음:
+ - **`fetch(ticker, periodDays)`** — 일봉 전용. 아래 3-tier 흐름 그대로. `predict` 와 차트 `D` 가 사용.
+ - **`fetchCandles(ticker, period)`** — 캔들 주기 진입 (2026-05-20 신규). `D` 는 `fetch` 재사용, `W/M/Y` 는 Yahoo native interval (`1wk`, `1mo`) 직접 호출 후 인메모리 캐시. `Y` 는 월봉을 받아 서버에서 연 단위 집계. DB 미저장 (daily-only 스키마 유지).
```

Layer 1 캐시 키 설명 갱신:
```diff
- **Layer 1 (메모리)**: 5분 TTL, `cacheKey = ticker_periodDays`
+ **Layer 1 (메모리)**: 5분 TTL. 일봉 경로는 `cacheKey = ticker_periodDays`, 캔들 주기 경로는 `candles_${ticker}_${period}` — 키 분리되어 충돌 없음.
```

## 4. 검증 (input ↔ output)

| 검증 | input | output | 판정 |
|---|---|---|---|
| 옛 `days` 표현 잔재 | grep `days` in stock-spec.md | 3건 잔존, 모두 `trainingDays`/`stockModel.getRange(days)` 등 별개 개념 — 정상 | ✅ |
| 신규 `period` 표현 도입 | grep `period\|fetchCandles\|캔들 주기` | 6곳 일관 적용 | ✅ |
| 코드 영향 | 문서 only 변경 | 코드 회귀 없음 | ✅ |

## 변경 파일

### 신설
- `000.aicowork/20260520/20260520-063901-Stock차트-재검토및문서정합.md` (본 문서)

### 수정
- `docs/stock-spec.md` — L44/L67/L79/L98-103/L120 (5곳, days→period + fetchCandles 진입점 명시)

## 다음 세션 진행 가이드

### 🟢 [1] 차트 UX 보강 (선택)
- 빈 응답 시 기존 차트 destroy + "데이터 없음" 메시지 표시
- invalid ticker → `{success:false, error:...}` 명시적 응답
- 요청 토큰으로 rapid-click 레이스 회피

### 🟢 [2] 데이터 라벨 정합 (선택)
- `config/stocks.js` L11: `051910.KS` → "LG Chem" (현실 정합)
- 필요 시 LGES `373220.KS` 별도 추가

### 🟢 [3] W 집계 KST 보정 (선택, 폴백 한정)
- `_aggregateFromDaily('W')` UTC Sunday → KST Monday 시작으로 변경

## 환경 상태

- git: main repo `E:\workspace\bingonode` = 브랜치 `main`
- 직전 푸시 `3fd4d4d` 이후 본 세션이 문서 후속 정합

## 검토 체크리스트

| 항목 | 상태 | 비고 |
|---|---|---|
| ✅ 엣지 케이스 8건 검증 | 완료 | 7건 통과, 1건 사전 존재 UX |
| ✅ 사전 존재 결함 식별·분류 | 완료 | 5건, 본 세션 범위 외 |
| ✅ stock-spec.md 정합 | 완료 | 5곳 |
| ✅ 정합 검증 | 완료 | grep 확인 |
| ➖ UX 보강 / 라벨 정합 / W KST | 가이드 | 사용자 결정으로 미수정 |

---

**세션 자체 진행률**: 재검토 100% / 문서 정합 100% / 세션 이력 100%.
