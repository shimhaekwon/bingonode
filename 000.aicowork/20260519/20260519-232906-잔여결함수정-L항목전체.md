---

* 작성일시: 2026-05-19T23:29:06+09:00
* 모델: Claude Opus 4.7 (1M context)
* 작업프로젝트:
  * E:\workspace\bingonode
* 작업 단위: 코드 검토 잔여 결함(L1~L7) 전체 수정 — 수정 범위를 C·H·M·L 전체로 확대
* 직전 처리:
  * [./20260519-231838-코드리뷰결함수정-6건.md](./20260519-231838-코드리뷰결함수정-6건.md) (C1·H2·H1·M1·M3·M4 — 6건)
* **상태**: Low 항목 전체(L1~L7) 수정 완료. 데드코드 3파일 삭제 + 의존성 안전 업그레이드 + 라우트/설정 정비. 앱 부팅·라우트·기능 검증 통과.

---

# 세션 이력: 2026-05-19 (밤) — 잔여 결함 L1~L7 전체 수정

## 작업 대상

- 저장소: `E:\workspace\bingonode`
- 적용 원칙: `E:\project\.claude\6_6-basis.md` (6-6 원칙)
- 범위 확대: 직전 세션은 C·H·M(6건)만 수정 → 본 세션에서 **C·H·M·L 전체** 완료 (사용자 지시)

## 작업 범위

직전 세션 미처리분 = Low 항목 7건(L1~L7) + M2(사용자 결정으로 유지).

## 6-6 원칙 — no guess 적용

| 항목 | 질문 | 사용자 결정 |
|---|---|---|
| L5 | 구버전 의존성 업그레이드 범위 | **Major 유지 · 안전 업그레이드** (major 버전 고정, minor/patch만) |
| L1 | "Keeping for reference" 명시된 데드코드 파일 처리 | **삭제** |

## 수정 상세

### L1 — 데드코드 제거

| 대상 | 처리 |
|---|---|
| `controllers/stockController.js` | **파일 삭제** (384줄 전체 주석 — deprecated Python 컨트롤러) |
| `routes/stockRouter.js` | **파일 삭제** (20줄 전체 주석 — deprecated Python 라우터) |
| `models/bingoModel.js` 하단 주석 블록 | **제거** (51줄 — 구버전 better-sqlite3 코드) |
| `queries/bingoQueries.js` `setCreate` | **제거** (미참조 쿼리 키) |
| `services/mandelPopularity.js` | **유지** — `bingo-spec.md`의 `popularity()` 명세와 정확히 일치하는 getPredict 예측엔진용 의도된 대기 코드 |

`app.js`의 stockRouter 관련 데드 주석 2줄(require·app.use)도 함께 제거.

### L2 — 미등록 라우트 보강

`bingo-spec.md`가 `POST /api/bingo/getRecent` 라우트 존재를 명세하나 `bingoRouter.js`에 누락되어 있었음.

```diff
  router.post('/getList', apiController.getList);
+ router.post('/getRecent', apiController.getRecent);
  router.post('/sync', apiController.postSync);
```

- 직전 세션 M1 수정으로 `getRecent` 컨트롤러/모델이 정상 동작 상태 → 라우트 추가로 즉시 사용 가능.
- `setUpsert`는 spec 라우트 목록에 없어 컨트롤러 메서드로만 유지(다름없이).

### L3 — 락파일 단일화

- `yarn.lock` **삭제**. README가 `npm ci` 사용을 명시 → `package-lock.json` 단독 유지.

### L4 — `app.js` dotenv 로드 위치 교정

```diff
+ require('dotenv').config();
  var createError = require('http-errors');
  ...
- require('dotenv').config();   // (기존: 74번째 줄, 라우터 require·부팅 IIFE 이후)
```

- 부수 효과: `bingoService.js`가 모듈 로드 시 읽는 `process.env.DEBUG_SYNC`가 이제 `.env` 적용 후 평가됨(기존엔 미적용 가능성).

### L5 — 의존성 안전 업그레이드 (major 유지)

```diff
- "debug": "~2.6.9",          → "^2.6.9"   (major 2 유지, 2.x 최신)
- "express": "~4.16.1",       → "^4.21.2"  (major 4 유지)
- "http-errors": "~1.6.3",    → "^1.8.1"   (major 1 유지)
- "morgan": "~1.9.1",         → "^1.10.0"  (major 1 유지)
```

`npm install` 후 실제 설치 버전 — express **4.22.2** / morgan **1.10.1** / http-errors **1.8.1** / debug **2.6.9**. 전부 기존 major 동일. `package-lock.json` 갱신.

### L6 — `nunjucks watch` 환경 조건부

```diff
  nunjucks.configure('views', {
    express: app,
-   watch: true,
+   watch: process.env.NODE_ENV !== 'production',
  });
```

- 프로덕션에서 불필요한 파일 감시(chokidar) 비활성화.

### L7 — 요청 body 로깅 절단

`app.js` `loggingMiddleware`:
```diff
- const reqBody = JSON.stringify(req.body);
+ const rawBody = JSON.stringify(req.body) || '';
+ const reqBody = rawBody.length > 500
+   ? rawBody.slice(0, 500) + `...(${rawBody.length} bytes)`
+   : rawBody;
```

- 대용량 body 전량 로깅 방지(로그 비대화 완화).

### M2 — 미수정 (사용자 결정)

- `.env.dev`/`.env.prod`는 직전 세션에서 **유지** 결정(타 플랫폼 사용). 변경 없음.

### 문서 정합

- `docs/bingo-spec.md` — getPredict 라우트 비활성화 상태 반영(2곳).

## 검증 결과 (input ↔ output 대조)

### ④ Code·Side-Effect·Existed-Code inspection

| 검증 | input(기대) | output(결과) | 판정 |
|---|---|---|---|
| 구문 검사 | app.js 외 수정 파일 `node --check` | 전부 OK | ✅ |
| 의존성 설치 | major 유지 업그레이드 | express 4.22.2 / morgan 1.10.1 / http-errors 1.8.1 / debug 2.6.9 | ✅ |
| 락파일 | package-lock.json 단독 | yarn.lock 제거 확인 | ✅ |
| 앱 부팅(NODE_ENV=production) | listen 성공 | `APP BOOT OK` | ✅ |
| `POST /api/bingo/getRecent` | 200 `{rows,total,limit,offset}` | `200 {"rows":[],"total":0,"limit":5,"offset":0}` | ✅ |
| `POST /api/bingo/getList` | 200 | `200 {"rows":[],"total":0,...}` | ✅ |
| `POST /api/bingo/getPredict` | 비활성화 → 404 (500 아님) | `404` (ReferenceError 미발생) | ✅ |
| `POST /api/calc/random` | 200 | `200 {"num1":82,"num2":63}` | ✅ |

**Side-Effect 검토**:
- 삭제 2파일(stockController/stockRouter)은 활성 코드에서 require되지 않음 — app.js 주석 참조까지 제거하여 dangling 없음.
- `setCreate` 제거 — 어떤 코드도 참조 안 함(grep 확인).
- L4 dotenv 이동 — `config/db.js`/`stockModel.js`는 자체 dotenv.config 보유, 영향 없음.
- L5 — 전부 major 동일 범위 업그레이드, 앱 부팅·라우트 응답으로 회귀 없음 확인.

### ⑤ No Issue? → **이슈 없음**

## 변경 파일

### 신설
- `000.aicowork/20260519/20260519-232906-잔여결함수정-L항목전체.md` (본 문서)

### 삭제
- `controllers/stockController.js` (deprecated Python — 전체 주석)
- `routes/stockRouter.js` (deprecated Python — 전체 주석)
- `yarn.lock` (락파일 단일화)

### 수정
- `app.js` — dotenv 최상단 이동, nunjucks watch 조건부, body 로깅 절단, stockRouter 데드 주석 제거
- `models/bingoModel.js` — 하단 주석 블록 51줄 제거
- `queries/bingoQueries.js` — 미사용 `setCreate` 제거
- `routes/bingoRouter.js` — `/getRecent` 라우트 추가
- `package.json` — 의존성 4건 안전 업그레이드
- `package-lock.json` — `npm install` 갱신
- `docs/bingo-spec.md` — getPredict 라우트 상태 갱신

## 다음 세션 진행 가이드

### 🟡 [1] getPredict 예측 엔진 구현
- `services/`에 `generatePredictions` 구현 (`mandelPopularity.js` 활용)
- `bingoController.getPredict`의 bare 식별자(`getRecent`/`getOne`)를 `bingoModel.*`로 정정
- 완료 시 `bingoRouter.js`·`bingo-spec.md`에서 getPredict 재활성화

### 🟢 [2] 선택적 후속
- `bingoController.setUpsert` 라우트 노출 여부 정책 결정
- 통합 테스트/CI 도입 (현재 테스트 코드 부재)

## 환경 상태

- git: main repo `E:\workspace\bingonode` = 브랜치 `main`
- Node: v24.13.0 / npm 설치 정상
- 본 세션 + 직전 세션으로 코드 검토 결함 C·H·M·L 전체 처리 완료 (M2는 사용자 결정으로 유지)

## 검토 체크리스트

| 항목 | 상태 | 비고 |
|---|---|---|
| ✅ L1 데드코드 제거 | 완료 | 2파일 삭제 + 주석 블록 정리 |
| ✅ L2 getRecent 라우트 | 완료 | spec 정합 |
| ✅ L3 락파일 단일화 | 완료 | yarn.lock 삭제 |
| ✅ L4 dotenv 위치 | 완료 | 최상단 이동 |
| ✅ L5 의존성 업그레이드 | 완료 | major 유지 |
| ✅ L6 nunjucks watch | 완료 | 환경 조건부 |
| ✅ L7 body 로깅 절단 | 완료 | 500자 캡 |
| ✅ 검증 (구문·설치·부팅·라우트) | 완료 | 8개 항목 전부 통과 |
| ➖ M2 .env 다중파일 | 유지 | 사용자 결정 |

---

**세션 자체 진행률**: L1~L7 수정 100% / 검증 100% / 문서 정합 100% / 세션 이력 100%.
